```markdown
# MultiplesOfFiveFinder

این پروژه یک برنامه‌ی ساده به زبان **C#** است که مضارب پنج را بین دو عدد واردشده توسط کاربر پیدا کرده و نمایش می‌دهد.

## ویژگی‌ها
- دریافت دو عدد صحیح از کاربر  
- اطمینان از اینکه عدد اول همیشه کوچک‌تر یا مساوی عدد دوم باشد (در صورت نیاز جابه‌جایی انجام می‌شود)  
- بررسی همه‌ی اعداد بین این دو عدد  
- چاپ مضارب ۵ به ترتیب صعودی  
- توقف مرحله‌ای بعد از نمایش هر مضرب (به دلیل استفاده از `Console.ReadKey()`)

## مثال اجرا
```
لطفاً عدد اول را وارد کنید:
7
لطفاً عدد دوم را وارد کنید:
26

مضارب ۵ بین این دو عدد عبارتند از:
10
15
20
25
```

## نحوه استفاده
1. ریپازیتوری را کلون کنید:
   ```bash
   git clone https://github.com/YourUsername/MultiplesOfFiveFinder.git
   ```
2. پروژه را در **Visual Studio** باز کنید یا با دستور `dotnet run` اجرا کنید.  
3. دو عدد وارد کنید تا مضارب ۵ بین آن‌ها نمایش داده شود.  

---

📌 این پروژه یک تمرین ساده و آموزشی برای یادگیری مفاهیم پایه‌ای **C#** مثل ورودی/خروجی، شرط‌ها و حلقه‌ها است.
